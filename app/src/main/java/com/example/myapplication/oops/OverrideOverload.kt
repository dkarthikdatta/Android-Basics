package com.example.myapplication.oops

/**
 * Override -> run time polymorphism -> decides which class function/method to be runned in run time
 * Overload -> compile time polymorphism -> decides which function/method to be runned in compile time
 */
class OverrideOverload {
}